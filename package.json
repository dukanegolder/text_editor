{
  "name": "text_editor",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
      "start": "cd client && npm install && npm run build && cd ../server && npm install && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
      "express": "^4.17.1"
  },
  "devDependencies": {
      "nodemon": "^2.0.4"
  }
}